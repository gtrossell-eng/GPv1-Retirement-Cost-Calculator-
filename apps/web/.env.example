# API base path or absolute URL for pricing API requests.
# Same-origin defaults should use /api (Azure Static Web Apps, reverse-proxy setups).
# Use a full URL only when frontend and API are hosted on different origins.
VITE_API_BASE_URL=/api
